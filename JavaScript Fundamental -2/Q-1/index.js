/* Problem: Explain the usage of ternary operator with syntax and writea program to check whetehr the number is even or odd using the ternary operator.

Solution And answer:

The ternary operator, also known as the conditional operator, is a concise way to write simple conditional statements in a single line. It has the following syntax:

conditon ? expressionIfTure : expressionIfFalse


Program
*/

let num = 3;

let result = num%2==0 ? true :false;

console.log(num);